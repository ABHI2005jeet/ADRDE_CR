# Backend Placeholder

This directory is reserved for future backend/API work. The current prototype is frontend-only and uses mock data from `src/mockData`.

Suggested future responsibilities:

- Authentication and role management
- Meeting CRUD APIs
- Agenda workflow APIs
- Document upload and storage integration
- Report generation APIs
- Notification delivery
